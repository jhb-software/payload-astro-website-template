# Repository Notes

Single TanStack Start app with Payload mounted at `/admin` and API routes under `/api`.

## Commands

```sh
pnpm dev
pnpm build
pnpm check
pnpm generate:types
pnpm generate:importmap
```

Keep the app small. Reuse Payload fields/blocks before adding plugins or custom APIs.
